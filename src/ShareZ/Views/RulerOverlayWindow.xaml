<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ShareZ.Views.RulerOverlayWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    Title="Screen Ruler">

    <Grid Background="Transparent">
        <canvas:CanvasControl x:Name="RulerCanvas"
                              Draw="RulerCanvas_Draw"
                              PointerPressed="RulerCanvas_PointerPressed"
                              PointerMoved="RulerCanvas_PointerMoved"
                              PointerReleased="RulerCanvas_PointerReleased" />

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top"
                    Margin="0,20,0,0" Background="#CC000000" CornerRadius="6" Padding="16,8">
            <TextBlock Text="Click and drag to measure" Foreground="White" FontSize="14"
                       HorizontalAlignment="Center" />
            <TextBlock Text="Press ESC to close" Foreground="#AAAAAA" FontSize="11"
                       HorizontalAlignment="Center" Margin="0,4,0,0" />
        </StackPanel>

        <Border x:Name="MeasureInfo" Background="#CC000000" CornerRadius="4" Padding="12,8"
                HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed">
            <TextBlock x:Name="MeasureText" Foreground="White" FontSize="16" FontFamily="Consolas" />
        </Border>
    </Grid>
</Window>
