<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ShareZ.Views.Pages.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="History" Style="{StaticResource TitleTextBlockStyle}" />

        <!-- Search and Actions -->
        <Grid Grid.Row="1" Margin="0,16,0,12" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SearchBox" PlaceholderText="Search history..."
                     TextChanged="SearchBox_TextChanged">
                <TextBox.HeaderTemplate>
                    <DataTemplate />
                </TextBox.HeaderTemplate>
            </TextBox>

            <Button Grid.Column="1" ToolTipService.ToolTip="Refresh" Click="Refresh_Click">
                <FontIcon Glyph="&#xE72C;" FontSize="14" />
            </Button>
            <Button Grid.Column="2" Content="Clear All" Click="ClearAll_Click" />
        </Grid>

        <!-- History List -->
        <ListView Grid.Row="2" x:Name="HistoryList" SelectionMode="Single"
                  SelectionChanged="HistoryList_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="8" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="48" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Border Width="48" Height="48" CornerRadius="4" Background="{ThemeResource LayerOnMicaBaseAltFillColorDefault}">
                            <FontIcon Glyph="&#xE8B9;" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                            <TextBlock Text="{Binding FileName}" FontWeight="SemiBold" />
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBlock Text="{Binding Timestamp}" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.5" />
                                <TextBlock Text="{Binding CaptureType}" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.5" />
                            </StackPanel>
                        </StackPanel>

                        <TextBlock Grid.Column="2" Text="{Binding FileExtension}" VerticalAlignment="Center"
                                   Opacity="0.5" Style="{StaticResource CaptionTextBlockStyle}" />

                        <StackPanel Grid.Column="3" Orientation="Horizontal" Spacing="4">
                            <Button ToolTipService.ToolTip="Open" Tag="{Binding}" Click="OpenFile_Click" Padding="4">
                                <FontIcon Glyph="&#xE8E5;" FontSize="12" />
                            </Button>
                            <Button ToolTipService.ToolTip="Open Folder" Tag="{Binding}" Click="OpenFolder_Click" Padding="4">
                                <FontIcon Glyph="&#xE838;" FontSize="12" />
                            </Button>
                            <Button ToolTipService.ToolTip="Copy Path" Tag="{Binding}" Click="CopyPath_Click" Padding="4">
                                <FontIcon Glyph="&#xE8C8;" FontSize="12" />
                            </Button>
                            <Button ToolTipService.ToolTip="Delete" Tag="{Binding}" Click="DeleteItem_Click" Padding="4">
                                <FontIcon Glyph="&#xE74D;" FontSize="12" />
                            </Button>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
